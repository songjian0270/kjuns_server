<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kjuns.mapper.VisitorMapper">

	<select id="get" resultType="com.kjuns.model.Visitor" >
		SELECT name,nationality,remark FROM kjuns_visitor where data_flag = 1 and  id = #{id}
	</select>
	
</mapper>